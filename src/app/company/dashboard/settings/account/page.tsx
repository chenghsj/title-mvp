import React from 'react';
import Account from '@/app/candidate/dashboard/settings/account/page';

type Props = {};

const AccountPage = (props: Props) => {
  return <Account />;
};

export default AccountPage;
